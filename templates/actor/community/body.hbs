<section class="community-body">
  <div class="community-section">
    <h3>{{localize 'CYPHER.Description'}}</h3>
    <prose-mirror name="system.description" value="{{system.description}}"></prose-mirror>
  </div>
  <div class="community-section">
    <h3>{{localize 'CYPHER.Community.Resources'}}</h3>
    <prose-mirror name="system.resources" value="{{system.resources}}"></prose-mirror>
  </div>
  <div class="community-section">
    <h3>{{localize 'CYPHER.Notes'}}</h3>
    <prose-mirror name="system.notes" value="{{system.notes}}"></prose-mirror>
  </div>
</section>
